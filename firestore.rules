rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Development: allow all authenticated users full access.
    // TODO: Tighten rules before production launch.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
